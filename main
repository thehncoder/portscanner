import socket
import termcolor

def scan_port(IP address,port):
  try:
       sock=socket.socket()#creates a socket
       sock.connect((IP address,port))#connection succeded
       print("Connecting",port,"port number....")
       print("YES! THE PORT IS OPEN")
  except:
      print("Connecting",port,"port number....")
      print("NO! THE PORT IS CLOSED")

def scan(t,p):
      for p in range(1,p)
      scan_port(target,p)
      
     
    
t=input("enter the targets for scanning (separate multiple targets by comma(,)")
po=input("enter how many ports")
if ',' in t:
    print("scanning multiple ports")
    for ip_addr in t.split(","):
        scan
        
        
    








